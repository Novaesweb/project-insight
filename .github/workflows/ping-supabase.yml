name: Ping Supabase

on:
  schedule:
    - cron: "3/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Chamar endpoint
        run: curl --fail -s https://webnovax.com/api/ping
