name: Ping Supabase

on:
  schedule:
    - cron: "0 12 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Chamar endpoint
        run: curl --fail -s https://webnovax.vercel.app/api/ping
